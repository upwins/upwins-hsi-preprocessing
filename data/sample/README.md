# Sample data placeholder

Drop a small example collection here so the notebooks run end to end:

- `raw_0_or` + `.hdr` — a raw cube containing the calibration panels (notebook 01)
- `raw_34850_or` + `.hdr` — a raw cube to convert to reflectance (notebook 02)
- a reflectance cube (e.g. `raw_4000_or_ref.img` + `.hdr`) to draw training ROIs on (notebook 03)

Or edit `config.yaml` to point at wherever you keep the full data.
